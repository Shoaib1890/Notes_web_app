<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes App</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-zinc-950 shadow-lg py-6 px-8 flex items-center justify-between">
      <h1 class="text-3xl font-bold text-white tracking-tight">📝 Notes App</h1>
      <span class="text-zinc-400 text-sm">Stay organized, stay productive</span>
    </header>

    <main class="flex-1 w-full max-w-5xl mx-auto p-6">
      <!-- Create Note Form -->
      <section class="bg-zinc-800 rounded-xl shadow-lg p-8 mb-10">
        <h2 class="text-2xl font-semibold text-white mb-4">Create a New Note</h2>
        <form action="/create" method="post" class="space-y-4">
          <input
            class="block w-full rounded-lg outline-none px-5 py-3 bg-zinc-900 text-white placeholder-zinc-500 focus:ring-2 focus:ring-blue-600 transition"
            type="text"
            placeholder="Title goes here..."
            name="title"
            required
          />
          <textarea
            class="block w-full rounded-lg outline-none px-5 py-3 bg-zinc-900 text-white placeholder-zinc-500 focus:ring-2 focus:ring-blue-600 transition resize-none"
            placeholder="Write your note details..."
            name="details"
            rows="4"
            required
          ></textarea>
          <input
            class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold px-5 py-3 rounded-lg cursor-pointer transition"
            type="submit"
            value="Create Note"
          />
        </form>
      </section>

      <!-- Notes List -->
      <section>
        <h2 class="text-xl font-semibold text-zinc-200 mb-4">Your Notes</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
          <% if(files.length > 0){ %> 
            <% files.forEach(function(val){ %>
              <div class="bg-zinc-800 rounded-xl shadow-md p-6 flex flex-col justify-between hover:scale-105 hover:shadow-xl transition-transform group">
                <h3 class="text-white text-xl font-bold truncate group-hover:text-blue-400 transition"> <%= val %> </h3>
                <div class="flex w-full justify-between items-center mt-4 gap-2">
                  <a class="text-blue-500 font-medium hover:underline" href="/file/<%= val %>">Read More</a>
                  <a class="text-zinc-400 hover:text-yellow-400 font-medium transition" href="/edit/<%= val %>">Edit</a>
                </div>
              </div>
            <% }) %> 
          <% } else { %>
            <div class="col-span-full text-center text-zinc-500 py-10 text-lg">No notes yet. Start by creating one above!</div>
          <% } %>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <footer class="bg-zinc-950 text-zinc-500 text-center py-4 mt-10 text-sm">
      &copy; <span id="year"><script>document.getElementById('year').innerText = new Date().getFullYear()</script></span> Notes App. Crafted with <span class="text-pink-500">♥</span> using Node.js & Tailwind CSS.
    </footer>
  </body>
</html>
