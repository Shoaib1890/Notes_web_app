<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Note | Notes App</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 min-h-screen flex flex-col">
    <main class="flex-1 flex items-center justify-center p-6">
      <div class="w-full max-w-md bg-zinc-800 rounded-xl shadow-lg p-8 text-white">
        <a class="text-blue-500 inline-block mb-8 hover:underline" href="/">&larr; Back to Home</a>
        <h2 class="text-2xl font-bold mb-6">Edit Note Filename</h2>
        <form action="/edit" method="post" class="space-y-4">
          <input
            class="block w-full rounded-lg text-zinc-400 outline-none px-5 py-3 bg-zinc-900 placeholder-zinc-500 focus:ring-2 focus:ring-yellow-500 transition"
            type="text"
            placeholder="Previous name"
            name="previous"
            value="<%= filename %>"
            readonly
          />
          <input
            class="block w-full rounded-lg outline-none px-5 py-3 bg-zinc-900 text-white placeholder-zinc-500 focus:ring-2 focus:ring-yellow-500 transition"
            type="text"
            placeholder="New name"
            name="new"
            required
          />
          <input
            class="w-full bg-yellow-600 hover:bg-yellow-700 text-white font-semibold px-5 py-3 rounded-lg cursor-pointer transition"
            type="submit"
            value="Update Name"
          />
        </form>
        <form action="/delete" method="post" class="mt-6" onsubmit="return confirm('Are you sure you want to delete this note?');">
          <input type="hidden" name="filename" value="<%= filename %>" />
          <button type="submit" title="Delete Note" class="flex items-center gap-2 bg-red-600 hover:bg-red-700 text-white px-5 py-3 rounded-lg shadow-sm font-semibold transition group focus:outline-none focus:ring-2 focus:ring-red-400 w-full justify-center mt-2">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-white group-hover:text-white">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
            Delete Note
          </button>
        </form>
      </div>
    </main>
  </body>
</html>
