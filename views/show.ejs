<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View Note | Notes App</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 min-h-screen flex flex-col">
    <main class="flex-1 flex items-center justify-center p-6">
      <div class="w-full max-w-2xl bg-zinc-800 rounded-xl shadow-lg p-10 text-white">
        <a class="text-blue-500 inline-block mb-8 hover:underline" href="/">&larr; Back to Home</a>
        <h1 class="text-4xl font-extrabold tracking-tight mb-4 text-blue-400 break-words"> <%= filename %> </h1>
        <div class="border-t border-zinc-700 pt-6">
          <p class="text-lg font-light leading-relaxed whitespace-pre-line"> <%= filedata %> </p>
        </div>
      </div>
    </main>
  </body>
</html>
